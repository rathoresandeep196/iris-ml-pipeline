# Iris ML Pipeline

This repo contains an Iris classifier using scikit-learn and a GitHub Actions workflow that runs tests automatically when a PR is opened.

## Structure

- `src/` – Training and evaluation scripts
- `tests/` – Unit test for evaluation
- `.github/workflows/` – GitHub Actions config
